# WINDOWS
# Caso ja tenha venv
.venv\Scripts\activate
pip install -r requirements.txt
python seed_data.py
python server.py

# Caso não tenha venv crie antes dos comandos acima atraves do codigo:
python -m venv .venv
